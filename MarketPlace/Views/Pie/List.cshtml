@*@model IEnumerable<MarketPlace.Models.Pie>*@
@*@model MarketPlace.ViewModels.PiesListViewModel*@
@model PiesListViewModel

<h1>@ViewBag.CurrentCategory</h1>

@foreach (var pie in Model.Pies)
{
    @*<div>
        <h2>@pie.Name</h2>
        <h2>@pie.Price.ToString("c")</h2>
        <h2>@pie.Category.CategoryName</h2>
    </div>*@

    <div class="col-sm-4 col-lg-4 col-md-4">
        <div class="thumbnail">
            <div class="caption">
                <h3 class="pull-right">@pie.Price.ToString("c")</h3>
                <h3>
                    <a>@pie.Name</a>
                </h3>
                <p>@pie.ShortDescription</p>
            </div>
        </div>
    </div>
}
